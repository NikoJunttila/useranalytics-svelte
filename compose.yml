services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: svelte-analytics
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
